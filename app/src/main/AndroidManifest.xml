<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AKS">
        <activity android:name=".inkilap_tarihi"></activity>
        <activity android:name=".ogretmenlik_uygulamalari" />
        <activity android:name=".bakanlik_teskilati" />
        <activity android:name=".devlet_memurlarinin_yargilanmasi_kanunu" />
        <activity android:name=".devlet_memurlari_kanunu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>